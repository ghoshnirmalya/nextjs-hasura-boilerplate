- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "public"."role"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "name" varchar NOT NULL, PRIMARY KEY ("id") , UNIQUE ("id"), UNIQUE ("name"));
  type: run_sql
- args:
    name: role
    schema: public
  type: add_existing_table_or_view
